# AGENTS.md

## Repository expectations
- 이 저장소는 Spring Boot 백엔드 프로젝트다.
- 모든 리뷰 코멘트는 한국어로 작성한다.
- 사소한 네이밍, 포맷팅, 스타일 차이는 최대한 지적하지 않는다.
- 실제 장애 가능성, 데이터 정합성, 성능 저하, 보안 문제를 우선한다.

## Important backend concerns
- JPA N+1 문제를 우선 점검한다.
- @Transactional 경계가 적절한지 반드시 본다.
- 외부 API 호출, 메시지 발행, 파일 업로드가 트랜잭션 내부에 있으면 주의 깊게 본다.
- 입력 검증과 예외 처리 누락을 점검한다.
- 동시성 문제와 중복 처리 가능성을 점검한다.
- 핵심 서비스 로직 변경인데 테스트가 없으면 지적한다.

## Output style
- 꼭 필요한 이슈만 짧고 명확하게 설명한다.
- 확신이 낮은 문제는 가능성으로 표현한다.
- 한 PR에 너무 많은 코멘트를 남기지 않는다.